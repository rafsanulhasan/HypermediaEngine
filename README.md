# Hypermedia Engine

This is a Hypermedia Engine that allows developers to make .NET APIs HATEOAS compatible. It works with objects, arrays and queryables.
It automatically generates links and metadata. Optionally It supports Entity Tag Caching.
